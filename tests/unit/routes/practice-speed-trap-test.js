import { test, moduleFor } from 'ember-qunit';

moduleFor('route:practice-speed-trap', 'Practice Speed Trap Route');

test('It exists', function(assert) {
  var route = this.subject();
  assert.ok(route);
});
